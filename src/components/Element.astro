---
const { name, emoji, draggable = true } = Astro.props;
---

<div
  class={`bg-gray-800 border border-gray-600 px-5 py-3 rounded-lg ${
    draggable ? "cursor-grab" : ""
  } select-none text-xl flex items-center gap-2`}
  draggable={draggable}
  data-element={name}
>
  <span class="text-2xl">{emoji}</span>
  <span class="capitalize">{name}</span>
</div>
